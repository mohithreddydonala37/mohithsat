export type Patient = { id: number; age: number; sex: string };
export type Report = { id: number; patient_id: number; filename: string; processing_status: string };
export type ReportUpload = Report & { pages: number; upload_date: string; report_metadata?: { original_filename?: string; processing_status?: string } };
export type ExtractedFact = { id: number; test_name?: string; name?: string; value?: string | null; unit?: string | null; reference_low?: number | null; reference_high?: number | null; reference_text?: string | null; observation?: string | null; source_page?: number | null; source_text?: string | null; verification_status: string; range_status?: string; origin: string; provider?: string; model?: string };
export type ReviewResponse = { report: ReportUpload; lab_results: ExtractedFact[]; medications: ExtractedFact[]; allergies: ExtractedFact[]; conditions: ExtractedFact[]; conflicts: Array<{ description: string; severity: string }>; provenance: Array<{ entity_type: string; entity_id: number; source_document?: string; source_page?: number; source_text?: string; verification_state: string }>; verification: Array<{ entity_type: string; entity_id: number; status: string }> };
export type PatientCreate = { age: number; sex: string; symptoms: string[]; existing_conditions: string[]; allergies: string[]; medications: string[] };
export type ApiClient = { getPatient: (id: number) => Promise<Patient>; getReport: (id: number) => Promise<Report>; createPatient: (patient: PatientCreate) => Promise<Patient>; uploadReport: (patientId: number, file: File) => Promise<ReportUpload>; extractReport: (reportId: number) => Promise<{ processing_status: string }>; getReview: (reportId: number) => Promise<ReviewResponse> };
